
--// Lazer Made Ts FN 🕶️
--// Safe GUI version

local Players = game:GetService("Players")
local TweenService = game:GetService("TweenService")
local ReplicatedStorage = game:GetService("ReplicatedStorage")

local Player = Players.LocalPlayer
local PlayerGui = Player:WaitForChild("PlayerGui")

--// Remote
local Event = ReplicatedStorage:FindFirstChild("307D0912-4609-4190-B07C-739143B666D3")

--// SETTINGS
local REQUEST_LIMIT = 909909090900000099009889800988
local DELAY = 0

local Colors = {
    Color3.new(0.33333334326744, 0, 0),
    Color3.new(0, 0, 0),
    Color3.new(0, 0.33333334326744, 0),
    Color3.new(0.33333334326744, 1, 1),
    Color3.new(0.66274511814117, 0.66666668653488, 0.50196081399998),
    Color3.new(0, 0, 0.49803921580315),
    Color3.new(0, 1, 0.0039215688593686),
    Color3.new(1, 0.33333334326744, 0),
    Color3.new(1, 1, 1),
    Color3.new(1, 0, 0.99607843160629),
    Color3.new(0, 0.33333334326744, 0.99607843160629),
    Color3.new(0.99607843160629, 0, 0)
}

--// GUI
local Gui = Instance.new("ScreenGui")
Gui.Name = "LazerMadeTsFN"
Gui.ResetOnSpawn = false
Gui.Parent = PlayerGui

local Main = Instance.new("Frame")
Main.Size = UDim2.fromOffset(350, 225)
Main.Position = UDim2.new(0.5, -175, 0.5, -112)
Main.BackgroundColor3 = Color3.fromRGB(3, 3, 3)
Main.BorderSizePixel = 0
Main.ClipsDescendants = true
Main.Parent = Gui

local Corner = Instance.new("UICorner")
Corner.CornerRadius = UDim.new(0, 14)
Corner.Parent = Main

--// Animated background
local AnimatedGlow = Instance.new("Frame")
AnimatedGlow.Size = UDim2.fromScale(1, 1)
AnimatedGlow.BackgroundTransparency = 0
AnimatedGlow.BackgroundColor3 = Color3.fromRGB(3, 3, 3)
AnimatedGlow.BorderSizePixel = 0
AnimatedGlow.ZIndex = 0
AnimatedGlow.Parent = Main

local GlowGradient = Instance.new("UIGradient")
GlowGradient.Color = ColorSequence.new({
    ColorSequenceKeypoint.new(0, Color3.fromRGB(255, 255, 255)),
    ColorSequenceKeypoint.new(0.25, Color3.fromRGB(3, 3, 3)),
    ColorSequenceKeypoint.new(0.5, Color3.fromRGB(255, 255, 255)),
    ColorSequenceKeypoint.new(0.75, Color3.fromRGB(3, 3, 3)),
    ColorSequenceKeypoint.new(1, Color3.fromRGB(255, 255, 255))
})

GlowGradient.Transparency = NumberSequence.new({
    NumberSequenceKeypoint.new(0, 0.94),
    NumberSequenceKeypoint.new(0.5, 0.98),
    NumberSequenceKeypoint.new(1, 0.94)
})

GlowGradient.Parent = AnimatedGlow

--// Smooth animation
task.spawn(function()
    local Rotation = 0

    while Gui.Parent do
        Rotation += 1

        if Rotation >= 360 then
            Rotation = 0
        end

        GlowGradient.Rotation = Rotation
        task.wait(0.03)
    end
end)

--// White neon outline
local Stroke = Instance.new("UIStroke")
Stroke.Color = Color3.fromRGB(255, 255, 255)
Stroke.Thickness = 2
Stroke.Transparency = 0.05
Stroke.Parent = Main

local Glow = Instance.new("UIStroke")
Glow.Color = Color3.fromRGB(255, 255, 255)
Glow.Thickness = 8
Glow.Transparency = 0.8
Glow.Parent = Main

--// Title
local Title = Instance.new("TextLabel")
Title.Size = UDim2.new(1, -60, 0, 35)
Title.Position = UDim2.fromOffset(15, 8)
Title.BackgroundTransparency = 1
Title.Text = "Lazer Made Ts FN🕶️"
Title.TextColor3 = Color3.new(1, 1, 1)
Title.TextSize = 20
Title.Font = Enum.Font.GothamBold
Title.TextXAlignment = Enum.TextXAlignment.Left
Title.ZIndex = 2
Title.Parent = Main

local Sub = Instance.new("TextLabel")
Sub.Size = UDim2.new(1, -30, 0, 25)
Sub.Position = UDim2.fromOffset(15, 42)
Sub.BackgroundTransparency = 1
Sub.Text = "NAME COLOR"
Sub.TextColor3 = Color3.fromRGB(170, 170, 170)
Sub.TextSize = 12
Sub.Font = Enum.Font.Gotham
Sub.TextXAlignment = Enum.TextXAlignment.Left
Sub.ZIndex = 2
Sub.Parent = Main

--// Middle toggle
local ToggleScript = Instance.new("TextButton")
ToggleScript.Size = UDim2.new(1, -30, 0, 48)
ToggleScript.Position = UDim2.fromOffset(15, 72)
ToggleScript.BackgroundColor3 = Color3.fromRGB(12, 12, 12)
ToggleScript.BorderSizePixel = 0
ToggleScript.Text = "NAME COLOR: OFF"
ToggleScript.TextColor3 = Color3.new(1, 1, 1)
ToggleScript.TextSize = 16
ToggleScript.Font = Enum.Font.GothamBold
ToggleScript.ZIndex = 2
ToggleScript.Parent = Main

local ButtonCorner = Instance.new("UICorner")
ButtonCorner.CornerRadius = UDim.new(0, 10)
ButtonCorner.Parent = ToggleScript

local ButtonStroke = Instance.new("UIStroke")
ButtonStroke.Color = Color3.fromRGB(255, 255, 255)
ButtonStroke.Thickness = 1.5
ButtonStroke.Transparency = 0.2
ButtonStroke.Parent = ToggleScript

--// Status
local Status = Instance.new("TextLabel")
Status.Size = UDim2.new(1, -30, 0, 30)
Status.Position = UDim2.fromOffset(15, 130)
Status.BackgroundTransparency = 1
Status.Text = "Requests: 0 / " .. REQUEST_LIMIT
Status.TextColor3 = Color3.fromRGB(200, 200, 200)
Status.TextSize = 14
Status.Font = Enum.Font.GothamMedium
Status.TextXAlignment = Enum.TextXAlignment.Left
Status.ZIndex = 2
Status.Parent = Main

--// Close
local Close = Instance.new("TextButton")
Close.Size = UDim2.fromOffset(35, 35)
Close.Position = UDim2.new(1, -40, 0, 8)
Close.BackgroundTransparency = 1
Close.Text = "×"
Close.TextColor3 = Color3.new(1, 1, 1)
Close.TextSize = 27
Close.Font = Enum.Font.GothamBold
Close.ZIndex = 3
Close.Parent = Main

--// Smaller Open button
local OpenButton = Instance.new("TextButton")
OpenButton.Size = UDim2.fromOffset(80, 34)
OpenButton.Position = UDim2.fromOffset(25, 250)
OpenButton.BackgroundColor3 = Color3.fromRGB(3, 3, 3)
OpenButton.BorderSizePixel = 0
OpenButton.Text = "Open"
OpenButton.TextColor3 = Color3.new(1, 1, 1)
OpenButton.TextSize = 13
OpenButton.Font = Enum.Font.GothamBold
OpenButton.Parent = Gui

local OpenCorner = Instance.new("UICorner")
OpenCorner.CornerRadius = UDim.new(0, 9)
OpenCorner.Parent = OpenButton

local OpenStroke = Instance.new("UIStroke")
OpenStroke.Color = Color3.new(1, 1, 1)
OpenStroke.Thickness = 1.5
OpenStroke.Parent = OpenButton

--// Draggable function
local function MakeDraggable(Object)
    local dragging = false
    local dragStart
    local startPosition

    Object.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1
            or input.UserInputType == Enum.UserInputType.Touch then

            dragging = true
            dragStart = input.Position
            startPosition = Object.Position

            input.Changed:Connect(function()
                if input.UserInputState == Enum.UserInputState.End then
                    dragging = false
                end
            end)
        end
    end)

    Object.InputChanged:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseMovement
            or input.UserInputType == Enum.UserInputType.Touch then

            if dragging then
                local Delta = input.Position - dragStart

                Object.Position = UDim2.new(
                    startPosition.X.Scale,
                    startPosition.X.Offset + Delta.X,
                    startPosition.Y.Scale,
                    startPosition.Y.Offset + Delta.Y
                )
            end
        end
    end)
end

MakeDraggable(Main)
MakeDraggable(OpenButton)

--// Notification
local function Notify()
    local Notification = Instance.new("Frame")
    Notification.Size = UDim2.fromOffset(310, 65)
    Notification.Position = UDim2.new(1, 330, 1, -85)
    Notification.BackgroundColor3 = Color3.fromRGB(3, 3, 3)
    Notification.BorderSizePixel = 0
    Notification.Parent = Gui

    local C = Instance.new("UICorner")
    C.CornerRadius = UDim.new(0, 12)
    C.Parent = Notification

    local S = Instance.new("UIStroke")
    S.Color = Color3.new(1, 1, 1)
    S.Thickness = 2
    S.Parent = Notification

    local Text = Instance.new("TextLabel")
    Text.Size = UDim2.new(1, -20, 1, 0)
    Text.Position = UDim2.fromOffset(10, 0)
    Text.BackgroundTransparency = 1
    Text.Text = "made by rovmb and rxlet on discord 👀"
    Text.TextColor3 = Color3.new(1, 1, 1)
    Text.TextSize = 8
    Text.Font = Enum.Font.GothamBold
    Text.Parent = Notification

    TweenService:Create(
        Notification,
        TweenInfo.new(0.4),
        {Position = UDim2.new(1, -325, 1, -85)}
    ):Play()

    task.wait(7)

    local Out = TweenService:Create(
        Notification,
        TweenInfo.new(0.4),
        {Position = UDim2.new(1, 330, 1, -85)}
    )

    Out:Play()
    Out.Completed:Wait()

    Notification:Destroy()
end

--// Toggle state
local Enabled = false
local Running = false

ToggleScript.MouseButton1Click:Connect(function()
    Enabled = not Enabled

    if Enabled then
        ToggleScript.Text = "NAME COLOR: ON"
        ToggleScript.TextColor3 = Color3.fromRGB(0, 255, 255)

        if not Running then
            Running = true

            task.spawn(function()
                local Sent = 0

                while Enabled and Sent < REQUEST_LIMIT do
                    for _, Color in ipairs(Colors) do
                        if not Enabled or Sent >= REQUEST_LIMIT then
                            break
                        end

                        if Event then
                            Event:FireServer("colo", Color)
                        end

                        Sent += 1
                        Status.Text =
                            "Requests: " .. Sent .. " / " .. REQUEST_LIMIT

                        task.wait(DELAY)
                    end
                end

                Running = false

                if Enabled then
                    Status.Text =
                        "Finished: " .. REQUEST_LIMIT .. " / " .. REQUEST_LIMIT
                end
            end)
        end
    else
        ToggleScript.Text = "NAME COLOR: OFF"
        ToggleScript.TextColor3 = Color3.new(1, 1, 1)
        Status.Text = "Requests: stopped"
    end
end)

--// Open / close
local Open = true

Close.MouseButton1Click:Connect(function()
    Open = false
    Main.Visible = false
end)

OpenButton.MouseButton1Click:Connect(function()
    Open = not Open
    Main.Visible = Open
end)

task.spawn(Notify)
